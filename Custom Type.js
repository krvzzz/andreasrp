module.exports = function({DBM, Actions, Files, Events, tempVars, serverVars, globalVars}, client){ // can put custom stuff that doesn't work properly in run scripts here! client.on("messageDelete", (message) => { const content = message.content; const author = message.author.tag; const channel = message.channel.name; const embed = { "title": "Message from " + author + " removed", "description": content, "color": 9902553, "footer": { "text": "In: " + channel } }; message.guild.channels.find('id', '564109490870288394').send({ embed }); }); }